/* 
Question: Write a function in Javascript that will reverse every 
word in a given string and return the new string. Every word should be 
reversed but the string as a whole should not be reversed. And DO NOT 
use array.reverse() method
*/

/*
1. given string, reverse every word 
2. return the new string 
3. string itself should not be reversed
*/

//Make a string 
var name="jennifer";

//have a variable with the same length as the string 
//this way, it can print the last index to index 0, 
//have the for loop decrease the len.
// return the whole string
for (var len = name.length-1; len >= 0; len--)
{
    return name;
}
